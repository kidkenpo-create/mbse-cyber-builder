# Copy to .env.local for local dev with `vercel dev`. NEVER commit real values.
ANTHROPIC_API_KEY=sk-ant-your-key-here
# Optional — override the model your key should use:
# ANTHROPIC_MODEL=claude-sonnet-4-20250514
# Optional — set a shared password to protect billing on a public deploy:
# ACCESS_PASSWORD=changeme
